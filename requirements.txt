# 런타임 의존성 (재현성을 위해 검증된 버전으로 고정)
anthropic==0.109.2
sentence-transformers==5.6.0
chromadb==1.5.9
streamlit==1.58.0
python-dotenv==1.2.2

# Streamlit Cloud 등 구버전 sqlite 환경 대응 (chromadb는 sqlite>=3.35 필요). 리눅스에서만 설치.
pysqlite3-binary>=0.5.0; platform_system == "Linux"
